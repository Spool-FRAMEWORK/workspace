# Normalise line endings so the shell scripts work on macOS and Linux
# regardless of the OS they were committed from.
* text=auto

*.sh   text eol=lf
mvnw   text eol=lf
*.cmd  text eol=crlf
*.ps1  text eol=crlf
